{
  "routes": [
    {
      "method": "GET",
      "path": "/hero-teasers",
      "handler": "hero-teaser.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/hero-teasers/count",
      "handler": "hero-teaser.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/hero-teasers/:id",
      "handler": "hero-teaser.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/hero-teasers",
      "handler": "hero-teaser.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/hero-teasers/:id",
      "handler": "hero-teaser.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/hero-teasers/:id",
      "handler": "hero-teaser.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
